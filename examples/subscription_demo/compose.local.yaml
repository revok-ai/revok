# Local development override — builds revok from source instead of pulling DockerHub.
# Usage: docker compose -f compose.yaml -f compose.local.yaml up --build

services:
  revok:
    image: ""  # clear the hub image
    build:
      context: ../..
      dockerfile: examples/subscription_demo/Dockerfile.revok
